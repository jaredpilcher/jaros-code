{"id":"fix_dedup_order","category":"fix","split":"dev","instruction":"dedup(xs) must remove duplicates while PRESERVING first-occurrence order. It currently uses set() which loses order. Fix it so dedup([3,1,3,2,1]) == [3,1,2].","target":"d.py","test_cmd":"python -m pytest -q","files":{"d.py":"def dedup(xs):\n    return list(set(xs))\n","test_d.py":"from d import dedup\n\n\ndef test_d():\n    assert dedup([3, 1, 3, 2, 1]) == [3, 1, 2]\n    assert dedup([]) == []\n    assert dedup(['a', 'a', 'b']) == ['a', 'b']\n"}}
