# jaros-code dependencies — Python 3.12+
#
# The harness uses ONLY the Python standard library plus the Jaros runtime.
# Inference runs remotely on the Jetson via llama.cpp's HTTP API (urllib, stdlib),
# so there are intentionally no heavyweight ML deps (no torch, no transformers).
#
#     pip install -r requirements.txt
#
# then point the client at your node:  export LLAMACPP_HOST=http://<jetson-ip>:8000

# ── runtime ──────────────────────────────────────────────────────────────────
jaros>=0.4.0          # the two-plane execution runtime jaros-code is built on (PyPI)

# ── test / dev only ──────────────────────────────────────────────────────────
pytest>=7.0
requests>=2.28        # used by tests/test_ext001_tools.py
