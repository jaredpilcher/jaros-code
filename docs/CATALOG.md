# jaros-code — Live Catalog

_Auto-generated by `scripts/gen_catalog.py` from the actual agents/tools/evals._
_Counts: **16 agents · 16 tools · 35 evals**._

## Agents (single-purpose reasoning, gemma2:2b)

- **`body-completer`** — Tiny single-purpose agent ``body-completer`` (EXT-002 / REQ-12).
- **`code-writer`** — Single-purpose agent ``code-writer`` (EXT-013 / REQ-1).
- **`commander`** — Single-purpose agent ``commander`` (EXT-002 / REQ-2).
- **`config-editor`** — Specialist agent ``config-editor`` (EXT-002 / REQ-6).
- **`dockerfile-editor`** — Specialist agent ``dockerfile-editor`` (EXT-002 / REQ-7).
- **`editor`** — Single-purpose agent ``editor`` (EXT-002 / REQ-1).
- **`gherkin-writer`** — Single-purpose agent ``gherkin-writer`` (EXT-012 / Jaros-native behavioral solve).
- **`markdown-editor`** — Specialist agent ``markdown-editor`` (EXT-002 / REQ-8).
- **`navigator`** — Single-purpose agent ``navigator`` (EXT-002 / REQ-5).
- **`orchestrator`** — Single-purpose agent ``orchestrator`` (EXT-004 / REQ-4).
- **`orchestrator-judge`** — Single-purpose judge-agent ``orchestrator-judge`` (EXT-013 / REQ-3).
- **`planner`** — Single-purpose agent ``planner`` (EXT-015 / plan-then-code decomposition).
- **`planner`** — Tiny single-purpose agent ``planner`` (EXT-004 multi-step): given a natural-language dev
- **`rewriter`** — Single-purpose agent ``rewriter`` (EXT-002 / REQ-4).
- **`test-reader`** — Single-purpose agent ``test-reader`` (EXT-002 / REQ-3).
- **`test-writer`** — Tiny single-purpose agent ``test-writer`` (EXT-008 / REQ-1).

## Tools (deterministic execution plane)

- **`code.apply_patch`** — Effectful execution-plane tool ``code.apply_patch`` (EXT-001 / REQ-4).
- **`fs.find`** — Read-only execution-plane tool ``fs.find`` (EXT-001 / REQ-10).
- **`fs.grep`** — Read-only execution-plane tool ``fs.grep`` (EXT-001 / REQ-3).
- **`fs.list`** — Read-only execution-plane tool ``fs.list`` (EXT-001 / REQ-2).
- **`fs.read`** — Read-only execution-plane tool ``fs.read`` (EXT-001 / REQ-1).
- **`code.generate_and_test`** — Execution-plane tool ``code.generate_and_test`` (EXT-012 / REQ-12).
- **`json.check`** — Read-only execution-plane tool ``json.check`` (EXT-001 / REQ-12).
- **`model.rewire`** — Execution-plane tool ``model.rewire`` (EXT-021, TASK-25).
- **`model.route`** — Execution-plane tool ``model.route`` (EXT-021, TASK-25).
- **`py.check`** — Read-only execution-plane tool ``py.check`` (EXT-001 / REQ-8).
- **`py.symbols`** — Read-only execution-plane tool ``py.symbols`` (EXT-001 / REQ-9).
- **`code.repair`** — Execution-plane tool ``code.repair`` (EXT-013 / REQ-2).
- **`code.augment_selftests`** — Execution-plane tool ``code.augment_selftests`` (EXT-012 / REQ-13).
- **`shell.exec`** — Effectful execution-plane tool ``shell.exec`` (EXT-001 / REQ-5).
- **`code.filter_strategy`** — Deterministic strategy filter (EXT-015 / plan-then-code decomposition).
- **`code.write_file`** — Effectful execution-plane tool ``code.write_file`` (EXT-001 / REQ-6).

## Eval tasks (by tier)

### Tier 1
- **add_sum** — The add function must return the SUM of a and b, but it currently subtracts (ret
- **clamp** — Implement clamp(x, lo, hi): return x constrained to the range [lo, hi] — lo if x
- **count_vowels** — Implement count_vowels(s): return the number of vowels (a, e, i, o, u, case-inse
- **factorial** — The fact function has an off-by-one bug: range(1, n) excludes n, so fact(5) retu
- **greet_format** — The greet function returns 'Hello NAME' but the test expects 'Hello, NAME!' (a c
- **is_even** — The is_even function is inverted: it currently returns True for odd numbers
- **max_two** — The maximum function should return the LARGER of a and b, but it returns the sma
- **reverse_string** — The reverse function should return the string reversed, but it returns it unchan
- **sum_js** — This JavaScript sum(a, b) function has a bug: it subtracts instead of adding
- **sum_list** — The total function should return the sum of a list of numbers, but it always ret
- **word_count** — Implement word_count(s): return the number of whitespace-separated words in s

### Tier 2
- **anagram** — Implement is_anagram(a, b): return True iff a and b are anagrams of each other i
- **binary_search** — search() is a binary search over a sorted list with a bug: the loop condition st
- **countdown** — countdown(n) should return the list from n down to 0 inclusive, e
- **dict_invert** — Implement invert(d): return a new dict mapping each value back to its key
- **dockerfile_fix** — Update the Dockerfile: change the base image to python:3
- **fibonacci** — Implement fib(n): return the nth Fibonacci number (0-indexed) where fib(0)==0, f
- **fizzbuzz** — Implement fizzbuzz(n): return a list of strings for the numbers 1
- **flatten** — Implement flatten(xss): given a list of lists, return a single list with all ele
- **gcd** — The gcd function should return the greatest common divisor of a and b, but it re
- **json_config** — The config
- **markdown_fix** — Fix README
- **palindrome** — Implement is_palindrome(s): return True iff s reads the same forwards and backwa
- **second_largest** — Implement second_largest(xs): return the second largest DISTINCT value in the li
- **title_case** — Implement title_case(s): capitalize the first letter of each whitespace-separate

### Tier 3
- **balanced_brackets** — Implement is_balanced(s): return True iff every bracket in s is correctly matche
- **caesar_cipher** — Implement encode(text, shift): shift each ASCII letter by `shift` positions with
- **longest_common_prefix** — Implement lcp(strs): return the longest common prefix string of a list of string
- **merge_intervals** — Implement merge(intervals): given a list of [start, end] intervals, merge all ov
- **merge_sorted** — Implement merge(a, b): given two ascending-sorted lists, return one merged ascen
- **my_atoi** — Implement my_atoi(s): parse a leading 32-bit signed integer from string s like C
- **roman_decode** — Implement from_roman(s): convert a Roman numeral string to its integer value, ha
- **roman_numerals** — Implement to_roman(n) converting an integer 1
- **run_length_encode** — Implement rle(s): run-length-encode a string as consecutive (char, count) pairs 
- **two_sum** — Implement two_sum(nums, target): return the indices [i, j] (i < j) of the two di

