{"id":"refactor_hard_recursive_rename","category":"refactor","split":"dev","instruction":"The private memoized recursive helper _memo_fib in fib.py has a name that no longer matches its purpose -- rename it to _cached_fib, including its own recursive calls to itself. Don't touch the public fib function's name. fib must keep behaving exactly as before.","target":"fib.py","test_cmd":"python -m pytest -q","files":{"fib.py":"_cache = {}\n\n\ndef _memo_fib(n):\n    if n in _cache:\n        return _cache[n]\n    if n <= 1:\n        result = n\n    else:\n        result = _memo_fib(n - 1) + _memo_fib(n - 2)\n    _cache[n] = result\n    return result\n\n\ndef fib(n):\n    return _memo_fib(n)\n","test_fib.py":"from fib import fib\n\n\ndef test_fib_values():\n    assert fib(0) == 0\n    assert fib(1) == 1\n    assert fib(10) == 55\n"}}